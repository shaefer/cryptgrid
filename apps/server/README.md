# @cryptgrid/server

Placeholder. The Colyseus server that wraps `packages/sim` arrives in **Milestone 3 — "Delve Together"** (see `docs/ROADMAP.md`). No code lives here until then, per the working agreements in `CLAUDE.md`.
