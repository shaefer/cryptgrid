/** The six primary attributes (docs/STATS.md). Values start single-digit, climb past 100 by mid-game. */
export interface Attributes {
  str: number;
  dex: number;
  cha: number;
  vit: number;
  wis: number;
  int: number;
}
